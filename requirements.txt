streamlit>=1.25
pandas
langchain>=0.0.350
langchain-groq
langchain-experimental
groq    # optional: if you plan to use groq sdk directly
faiss-cpu  # or faiss (for vectorstore) — pick faiss-cpu for most setups
openai  # required if using OpenAI embeddings
python-dotenv